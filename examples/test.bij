dec add :: Int -> Int -> Int
def add x y = prim<add x y>

dec putInt :: Int -> Int
def putInt i = ccall<printf "%i\n" i>

def doubleClosure x =
    let tmp = add x
    in tmp x

def doublePartial x =
    letrec def tmp y = add x y
    in tmp x

export main
def main x = putInt (doubleClosure 5)